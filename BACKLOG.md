# Project Backlog

## Epics

- specs/epic-base-next/ — DONE — seeded by /base:backlog init 2026-05-12
- specs/epic-lean-lead-decider/ — DONE — seeded by /base:backlog init 2026-05-12
- specs/epic-pipeline-autonomous-retros/ — DONE — completed 2026-05-12
- specs/epic-next-modes/ — DONE — completed 2026-05-12
- specs/epic-fast-track-routing/ — DONE — completed 2026-05-13
- specs/epic-next-auto-backlog-audit/ — IN_PROGRESS — created 2026-05-14

---

## Findings

- spec-epic-proposed-supersedes-composability-ready [scope:any] — `specs/epic-proposed-supersedes-composability/spec.md` — [DEFERRED:legacy-orphan:Is spec for epic-proposed-supersedes-composability rea…] Is spec for epic-proposed-supersedes-composability ready? Auto-dispatch aborted: `## Solution` and `acceptance-criteria.md` are entirely un-authored stubs requiring human input. Finish specifying then remove this finding. (2026-05-13)
- spec-epic-proposed-supersedes-composability-ready-2 [scope:any] — `specs/epic-proposed-supersedes-composability/spec.md` — [DEFERRED:legacy-orphan:Is spec for epic-proposed-supersedes-composability rea…] Is spec for epic-proposed-supersedes-composability ready? Auto-dispatch aborted: `## Solution` section is empty — the spec stub was promoted from the backlog finding but not yet authored. Finish specifying then remove this finding. (2026-05-13)
- proposed-supersedes-composability-undocumented [scope:base-plugin] — `plugins/base/skills/adr/SKILL.md:65-69` — [DEFERRED:spec-gap:spec stub `## Solution` and acceptance-criteria.md un-auth…] `proposed+supersedes` composability is undocumented: when both flags are passed together, it is unspecified whether the superseded ADR's `Superseded by:` line should be updated immediately (when the new ADR is still Status: Proposed) or deferred until the user accepts the ADR — surfaced as VQ-S4-009 PARTIAL, pipeline-autonomous-retros S4 (2026-05-12)
- skills-languages-markdown-md-exists-verification [scope:any] — `-` — [DEFERRED:spec-gap:## Solution and acceptance-criteria.md are entirely un-aut…] No `skills/languages/markdown.md` exists; the verification-examiner's AC-derived SPEC protocol requires "(b) tests at file:line" and "(c) confirmation tests are not pure mock-spy proxies", written for executable code — epics whose stories edit only Markdown files have no matching language skill, causing examiners to improvise — surfaced during epic-next-modes (2026-05-12)
- mode-3-verification-question-schema-split [scope:base-plugin] — `plugins/base/agents/story-planner.md` — [DEFERRED:spec-gap:spec stub `## Solution` and acceptance-criteria.md un-auth…] Mode-3 verification-question schema is split between two conflicting shapes: the system-prompt canonical shape uses `"question_id"/"category"/"ac_id"/"phase"` (no `check_method`), while the task-specific prompt uses `"id"/"type"/"ac_ref"/"check_method"/"status"`; every on-disk `verification.json` uses `"type"` not `"category"`, and `check_method` is absent from the system-prompt schema; recommendation: adopt the richer task-specific shape, rename `id→question_id` and `ac_ref→ac_id` to align vocabulary, ship `verification.schema.json` so drift is caught at write time; update `base:verification-examiner` in the same change — surfaced across epic-next-modes and epic-pipeline-autonomous-retros Mode-3 planning (2026-05-12; enriched 2026-05-12)
- mode-1-story-planner-should-require [scope:base-plugin] — `plugins/base/agents/story-planner.md` — Mode-1 story-planner should require a reconciliation pass against `exploration.json` before finalizing ACs: AC-MANIFEST-1 in epic-base-next pointed at non-existent commands listings in plugin.json/marketplace.json that the explorer had already flagged as absent, but the AC was not updated; add a Mode-1 step that cross-checks every AC's referenced artifact (file paths, manifest keys, config entries) against explorer findings — surfaced during epic-base-next pre-implementation (2026-05-12)
- verifier-cannot-distinguish-pre-existing-diff [scope:base-plugin] — `plugins/base/commands/feature.md` — verifier cannot distinguish pre-existing diff hunks from story-introduced changes: both S2 and S3 verifiers in epic-base-next raised severity-7 scope violations on pre-existing uncommitted hunks in `plugins/base/commands/bug.md`, requiring lead-side reconciliation twice; fix: at `/base:feature` startup, snapshot `git status -s` plus per-file hunk locations for any `M`-status file, persist as `epic-state.json.pre_existing_diff` (or equivalent), and pass the baseline into `base:verification-examiner` so the examiner subtracts pre-existing hunks before scoring scope-boundary ACs — surfaced across epic-base-next S2 and S3 (2026-05-12)
- all-three-mode-prompts-disagree-with [scope:base-plugin] — `plugins/base/agents/story-planner.md` — all three Mode prompts disagree with their corresponding on-disk schemas or de facto conventions (Mode 1: AC-ID uniqueness rule vs per-tag de facto; Mode 2: S1/S2 prose IDs vs `^[0-9]{2}$` schema pattern; Mode 3: `category` example vs `type` convention); each was surfaced individually in the same epic run (epic-next-modes), suggesting prompts and schemas were authored independently and have never been reconciled as a set; add a schema-conformance pass to story-planner setup: for each Mode, validate the prompt's stated examples against the corresponding schema before shipping — surfaced during epic-next-modes pre-implementation (2026-05-12)
- owning-module-single-string-blocking-expression [scope:base-plugin] — `plugins/base/schemas/stories.schema.json` — `owning_module` is a single string, blocking expression of cross-file atomicity; in epic-pipeline-autonomous-retros S2 (Feature command + Bug command), the planner had to assign a primary module and bury the second in `notes` — extend the schema so `owning_module` may be a string or an array of module identifiers; update story-planner Mode 2 and spec-template reference accordingly — surfaced during epic-pipeline-autonomous-retros pre-implementation (2026-05-12)
- mode-3-vq-literal-string-grep [scope:base-plugin] — `plugins/base/agents/story-planner.md` — Mode-3 VQ literal-string grep lint is missing: VQ-S3-007 in epic-pipeline-autonomous-retros asked for "zero AskUserQuestion occurrences" but the architect rephrased frontmatter to "Fully non-interactive" to avoid a false-positive on "No AskUserQuestion" — the check passed correctly but only by architect awareness; add a Mode-3 lint: when `check_method` contains `grep`, require the question prose to state the exact literal token being searched rather than a semantic paraphrase or negation — surfaced during epic-pipeline-autonomous-retros verification (2026-05-12)
- skip-condition-one-off-interactive-debugging [scope:base-plugin] — `plugins/base/skills/e2e-testing/SKILL.md:20-21` — SKIP condition "one-off interactive debugging where sequential execution adds no value" is too easy to misapply: repeated single-file targeted runs (e.g. `npx playwright test e2e/46-...`) get reclassified as debugging when they are in fact repeated suite invocations; practical consequence was 11 tests running in parallel against a shared relay each time, full output flooding main context, and no fail-fast discipline; tighten the SKIP condition to require *truly* one-off (≤1 run, non-suite) or add a heuristic: if the same test file is invoked more than once in a session, escalate to the skill — surfaced in a nostr-skills session (2026-05-13)
- base-bug-leaves-working-tree-dirty [scope:base-plugin] — `plugins/base/commands/bug.md` — `/base:bug` leaves working tree dirty after ACCEPTED: when a run terminates ACCEPTED with no escalation, no rejection, and no open remediation rounds, the natural terminal state is "committed to branch" not "staged for review"; Step 4 should auto-commit exactly the files in the contract's `allowed_changes.files` plus bug-reports artifacts and BACKLOG.md, using the bug slug + root_cause for the commit message; skip auto-commit on any non-ACCEPTED terminal state (ESCALATED, REJECTED, ABORT:UNDERSPECIFIED); surfaced by user after shophop/waitForTimeout-has-no-durable-fallback run (2026-05-13)
- backlog-marker-design-fails-when-multiple [scope:base-plugin] — `plugins/base/skills/backlog/references/format.md` — BACKLOG marker design fails when multiple findings share an anchor: `/base:next` Step 5 says "extend marker until unique," but the wire format `backlog:<marker> auto` carries a single whitespace-delimited token, so multi-token markers cannot pass through to `base:feature`/`base:bug`. Line numbers as a fallback are unsafe — the marker also lives in the deferred-state stamp (`[INSUFFICIENT:]`/`[ALREADY-RESOLVED:]`) that the next `/base:next` reads to skip the bullet, and any benign backlog edit (add-finding, resolve-and-remove, a stamp landing on a different bullet) shifts line numbers and orphans the loop-break invariant. Proposal: slug-keyed bullets — coin a 3–6 word kebab-case identifier at write time, place it in the bullet as `- <anchor> — [<slug>] <text> (YYYY-MM-DD)`, use as the dispatch marker (`backlog:<slug>`); uniqueness enforced at write time by deterministic derivation from finding text with a discriminator suffix on collision; slug stays in position 1, deferred-state stamp lives in position 2 (`[<slug>] [INSUFFICIENT: …] <prose>`); detector distinguishes slugs (no colon) from stamps (colon required). Scope: grammar change here in `format.md`; reader-side parsers in `plugins/base/commands/{next,feature,bug}.md` (collapses Step-5 extend-until-unique into a single grep); writer-side wiring in `plugins/base/skills/backlog/SKILL.md`, `plugins/base/agents/project-curator.md`, `plugins/base/agents/retro-synthesizer.md`; one-shot migration of existing BACKLOG.md bullets (and resolution of pre-stamp-redesign `Auto-dispatch aborted:` legacy orphans in the same sweep); graceful fallback for hand-edited or pre-migration bullets via anchor-grep + one-line nag. Failure mode demonstrated 2026-05-14: `/base:next auto` selected the first actionable bullet anchored at `plugins/base/agents/story-planner.md`, dispatched with the non-unique anchor as marker, `base:feature` BACKLOG_PROMOTE correctly aborted on the 4-way ambiguity, and no forward progress occurred; no fallback stamp could land because the marker was ambiguous for stamping too (2026-05-14)

---

## Archive

- _no rejections yet_
